/**
 * Single swap point for the portrait.
 *
 * When the photo lands in src/assets/portrait.png, uncomment the import and
 * export it — every section that renders the portrait picks it up from here.
 *
 *   import portrait from "@/assets/portrait.png";
 *   export const PORTRAIT: string | null = portrait;
 */
export const PORTRAIT: string | null = null;
