# Telegram contact-form delivery.
# Create the bot with @BotFather, then message the bot once and read the
# chat id from https://api.telegram.org/bot<TOKEN>/getUpdates
#
# Never commit the real values — .env is gitignored.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
